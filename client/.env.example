# Not needed for normal local dev (Vite proxies /api to localhost:4000 automatically)
# or for the Vercel deployment (the client calls same-origin /api/listing).
# Only set this to override the proxy target, e.g. to point local dev at a deployed API.
# VITE_API_URL=https://your-deployment.vercel.app
